## Steps to use this:

1) Replace time_data.json with your own json export (with the same name)
2) Export settings:
   * Use project time
   * Include session metadata
   * Raw on/off pairs
   * Group by activity
